const initialState = {
    homeData: [], // Initial state
};

const goalsReducer = (state = initialState, action) => {
    switch (action.type) {
        // Define cases for different actions that update state
        default:
            return state;
    }
};

export default goalsReducer;